fragment NftItemFragment on NftItem {
    id
    name
    description
    src: preview(resolution: X500)
    address {
        rawFormat
        frienlyFormat
    }
    owner {
        address {
            frienlyFormat
        }
    }
}
